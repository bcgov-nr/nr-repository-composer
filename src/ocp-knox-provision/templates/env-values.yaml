intention:
  service:
    name: "<%= projectName %>"
    project: "<%= serviceName %>"
    environment: "<%= environment %>"
  user:
    name: "<%= intentionUser %>"
<% if (syncSecretEnabled) { -%>

sync:
  enabled: true
  vaultPaths: "<%= syncVaultPaths %>"
  secretNames: "<%= syncSecretNames %>"
<% } -%>